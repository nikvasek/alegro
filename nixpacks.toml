[phases.setup]
nixPkgs = ["chromium", "nss", "nss_latest", "nspr", "atk", "gtk3", "gtk2", "gdk-pixbuf", "glib", "pango", "cairo", "freetype", "fontconfig", "libX11", "libXext", "libXrender", "libXtst", "libXi", "libXdamage", "libXcomposite", "libXfixes", "libXrandr", "libxcb", "libxkbcommon", "libdrm", "mesa", "libGL", "alsa-lib", "dbus", "cups", "expat", "zlib", "libpng", "libjpeg", "libtiff", "libwebp", "wget", "gnupg", "apt"]

[phases.install]
cmds = [
  "apt-get update && apt-get install -y wget gnupg software-properties-common",
  "wget -q -O - https://dl.google.com/linux/linux_signing_key.pub | apt-key add -",
  "echo 'deb [arch=amd64] http://dl.google.com/linux/chrome/deb/ stable main' > /etc/apt/sources.list.d/google-chrome.list",
  "apt-get update && apt-get install -y google-chrome-stable",
  "wget -O /tmp/chromedriver.zip https://chromedriver.storage.googleapis.com/114.0.5735.90/chromedriver_linux64.zip",
  "unzip /tmp/chromedriver.zip -d /usr/bin/",
  "chmod +x /usr/bin/chromedriver",
  "google-chrome --version",
  "chromedriver --version"
]

[phases.build]
cmds = [
  "python -m pip install --upgrade pip",
  "pip install -r requirements.txt"
]

[start]
cmd = "python run_bot.py"